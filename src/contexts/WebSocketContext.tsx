
import { createContext } from 'react';
import { WebSocketProvider as WebSocketProviderComponent } from '@/hooks/use-websocket';

// Re-export the WebSocketProvider component as default
export default WebSocketProviderComponent;
